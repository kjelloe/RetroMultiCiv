// TECH_BLURBS — one ORIGINAL 1-2 sentence line per advance, shown on the
// discovery card (ui/discovery-card.js) and, later, folded into the pedia
// tech entries (one source). CONTENT RULES (the license boundary, CLAUDE.md):
// original prose only — never wiki sentences, never close paraphrase; the
// advice.js "short + original" standard; keep each under ~200 chars (the
// coverage gate enforces the cap and warns on missing ids).
//
// The table starts EMPTY-TOLERANT: the card renders name/era/unlocks without
// a blurb until the ally's authored 68 lines drop in as pure data here.
export const TECH_BLURBS = {
  // 'alphabet': 'Example shape — replace with the ally's authored line.',
};

# Away-window 4 brief (2026-07-25, ~7h) — TWO markers, XVII closed

## The headline

**Marker-0101 is the redeploy candidate** (`c51dceb`, MERGE-CONSISTENT,
clean-clone `--full` GREEN, 886 tests/0 fail): **refinement-XVII is
COMPLETE end-to-end** — all 22 of your playtest items — plus the runK
roblox batch and the ack-parser fix. The engine goldens are untouched
across 0097→0101 (four consecutive golden-neutral markers).

## The window's story, short

1. The reviewer's gate GREEN on hardening's join-toggle → merged →
   **marker-0100** tagged with consistency PENDING (the new
   post-0098 discipline).
2. The clean-clone gate found exactly ONE deterministic red: the XVII
   swamp-pond color missing from the regenerated
   specs/render-spec.json (the drift guard working). Fixed in
   minutes (the helper independently produced the identical fix);
   re-gate GREEN → **marker-0101** tagged + declared.
3. Prep work banked while quiet: **reports/v1-rc-draft.md** (the RC
   evidence digest, axes pre-filled — the ⏳ rows name exactly what
   remains) and **specs/readme-v1-draft.md** (the RC README assembled
   around the ally's verbatim three blocks, title-swappable). Test
   counts synced (886), memories current.

## Needs you (in order of payoff)

1. **Redeploy from marker-0101** — everything from yesterday+today
   goes live (late-join, QR join links, all 22 XVII items, the tile
   art, find-a-game fix). Bake step already in your ssh-deploy.sh.
2. **Fresh bugfixer session** — the engine runway is loaded: A8
   threading (kit banked) → coastal-build → RIVER terrain →
   D3-surfacing → D4–D6. This is the whole v1 critical path now.
3. **Fresh helper session** — #34 Founder's Record (+ flow-4).
4. **Roblox Write approval** — the intro animation file is authored
   and blocked on one click in that session.
5. **The Studio session** (whenever) — publish gate + the
   accumulated visual acceptances (specials, intro beats, items
   4b/4c).
6. Standing: trademark search + domains.

## Fleet notes

The lane-watcher carried the whole window (every reviewer/roblox/
sim-runner wake). Two tool fixes landed on the way: the ack-parser
all-decimal-hash bug (a real redelivery loop the helper caught) and
the earlier reaper cooldown. One friction remains: per-wake
permission variance on the gaming PC (the reviewer's wakes sometimes
carry full permissions, sometimes agent-mail-only) — a standing
grant in each watched clone's settings would end it.

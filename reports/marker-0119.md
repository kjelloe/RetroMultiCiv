# marker-0119 — period helmets + roof variation (H9); era-aging planned (H10)

**Tag:** `marker-0119` · **merge-consistent — the user may merge this**
(supersedes marker-0118). Client-only, High-gated, Low byte-verified.

- **H9a — helmets from the user's reference chart** (`debugging/helmets.png`):
  each unit type wears its period helmet — nasal helm on militia/legion,
  barbute on the phalanx, a true flat-topped great helm on knights, kettle
  hats on cavalry riders, a combed morion on musketeers; riflemen keep the
  steel brim.
- **H9b — roof-style variation** on peaked eras, per-house deterministic:
  era default / terracotta tile with ridge cap / grey-green shingle / log
  roofs with plank lines. User pick: A "balanced mix" of 3.
- **H10 — PLANNED ONLY** (user: "plan, but don't do yet"): cities aging
  Ancient → Space at High — per-band house kits (mud+thatch → timber+tile →
  brick+lamps+lit windows → glass+solar+landing-pad capital) atop the
  existing fog-honest era bands. No engine/state change needed. Awaiting go.

Test state: battery 22/22, Low byte-identical, review page carries the H9
shots (`debugging/h6-h8-variants.html`).

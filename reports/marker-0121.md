# marker-0121 — the H11 batch: seven user rulings, one landing

**Tag:** `marker-0121` · **merge-consistent — the user may merge this**
(supersedes marker-0120). Client-only; Low byte-verified; Medium changes
by design (the promotion below).

1. **R15 + period helmets promoted to Medium** — integrated-GPU players
   get the full figure work; the terrain split (faceted vs smooth) remains
   the tier distinction.
2. **Wonder landmarks at High** — six iconic wonders render on the owning
   city (pyramids, great wall, colossus, lighthouse, hanging gardens,
   oracle), fog-honest via view.wonders. The first draft anchored them
   north and the review shot caught them hidden — all anchors now sit on
   the camera-visible rim (the H8b lesson, applied twice now).
3. **Three classical banner poles** with faction flags.
4. **Richer High water** — wave-displaced sheet with moving glint + a
   counter-drifting shimmer layer; Low/Medium keep the exact shipped plane.
5. **playtest.md** — the device matrix and 1440p fps procedure for the
   user's multi-device pass; results land in the spec's §6.
6. **First-visit onboarding arrow** to the setup Graphics picker.
7. **`?gfx=` link param** — pre-selects and persists the tier from shared
   links, then canonicalizes away. (Auto-detect already ships via the GPU
   probe; no user-agent sniffing wanted.)

Test state: battery 22/22 (render-spec regenerated), graphics spec 3/3,
Low byte-identical. Review shots in `debugging/h6-h8-variants.html` (H11
section).

# marker-0086 — the AI settles overseas + the advisor speaks (MERGE-CONSISTENT)

Tagged at `f836d4e` (2026-07-22 afternoon). **MERGE-CONSISTENT —
supersedes 0085. Current merge candidate** (18th consecutive,
0069–0086). Reviewer TAG-GO (#2213) on BOTH gates: clean-clone 767/764
zero-real-fails + the tag-grade 25-seed M-floor cross-check.

## What changed (delta since 0085)

1. **Naval presence-1** (`f836d4e`, ruling #2201): the dormant slice-A
   settle-overseas core ARMS in simulation — M1 saturation bootstrap
   (an island-saturated coastal city with no free carrier builds one;
   breaks the circular dependency at its first link), M2 sea-only
   frontier exploration, M3 carrier-safe pathing (trireme-class hulls
   route land-adjacent only — the authentic open-sea sink honored;
   sail+ crosses open ocean), M2b rendezvous fix. seaPathRadius knob in
   rules.json; full re-record both engines (natural 510 rounds,
   `0x502920f0`); a NEW archipelago sim-smoke witness joins the twins
   gate (JS==Luau `0xda654d72`). Overseas cities now emerge on
   archipelago seeds vs the 0/5 baseline.
2. **The advisor speaks real words** (`08505cf` + `0970c91`): the
   15-trigger onboarding advisor shipped and then received the
   designer ally's card copy verbatim + 4 new pedia concepts
   (exploration / diplomacy / pollution / victory — the victory text
   ships title-neutral per the branding amendment).
3. **Helper batches** (`c4ab83e`, `ea0c995`, `d1623ae`, `0970c91`):
   XIV batch-2 (§32–§38) CLOSED incl. the road-goto stale-premise
   verification; batch-3 §41 military overview (with a shared
   overview-panel component) + §42 priority-driven settler
   auto-improve; §35 zoom-to.
4. **Master index public** (`34a212b`…): servers.multiciv.kjell.today
   as the baked Find-game default (?master=off opt-out), nginx/certbot
   deltas in the deploy docs, docs/16 §6 re-assessment (alias, safe);
   entry-flow + §7 capital-settler rulings recorded.
5. **Process**: the behavior-hash discriminator designed + queued (the
   stamp-vs-behavior finding); sim-runner parallel-capacity standing
   rule + the 3-arm King/prince/long-horizon measure-first portfolio
   armed; three-strikes counters advanced (thin-sample class at 2).

## The floor story (how the gate held honestly)

The 7-seed short-form showed M3-pop 27<28 and was NOT accepted at face
value: the stamp-vs-behavior analysis proved presence-1 byte-identical
to HEAD on the floor config (naval dormant there; the knob moves only
the cosmetic rulesetHash stamp), a stashed-tree HEAD baseline
reproduced the identical table, and the reviewer's independent 25-seed
cross-check confirmed median ~47 — the thin-sample class's second
strike, counted. No re-pin; the seed-12 unit-bloat tripwire was proven
pre-existing via a PAIRED HEAD run (identical tripwire, byte-identical
trajectory).

## Gates

Reviewer clean-clone + engine-diff GREEN with the #2201 checklist
(deterministic Q4 trigger, no state persistence, knob data-driven,
twin bit-exact on the naval-active path) + the 25-seed cross-check.
Gate-B: the archipelago witness is IN the twins gate at this tag; a
sim-runner run at f836d4e upgrades it to real (invited).

## Next

Engine: measure-first harness (#27, sim-driver free now) → sim-runner's
3-arm King portfolio (the space answer) → presence-2 (M4 + the
useless-carrier gating) → invade slice B → archetype wonders (ally
mapping on file). Client: helper resumes #25 build-queue UX.

# markers 0039–0042 — the A51 arc + wiki backstop (collective report)

Four golden-neutral markers, each consistent and standalone; collective
report per the night2-morning precedent.

## marker-0039 — A51a: master-index service
`tools/master.js` (plain node, zero deps): POST /announce with per-IP rate
floor + 4KB body cap, reachability probe that REJECTS loopback/private/
link-local targets before fetching (no outbound-relay abuse), GET /servers
public listings, ~3min TTL sweep, restart-forgets-all semantics.
test/master.test.js 7 cases.

## marker-0040 — A51b: server announce heartbeat
server/index.js: /healthz liveness route + a fenced OUTBOUND-ANNOUNCE region
(~60s POST: name, host:port, protocol version, the eight data checksums,
open public game count) + `--announce`/`--public-name` flags. Connect/cmd
dispatch untouched — the docs/17 hardening-lane boundary respected by
construction. test/server-announce.test.js 3 cases.

## marker-0041 — A51c: the client global tab (CLOSES A51)
client/ui/lobby.js + style.css: `?master=<url>` (module-eval capture per the
A45 trap, localStorage persistence), the 🌍 GLOBAL section — rows with
name/open-count/age, EIGHT-HASH version match (mismatches greyed with a
plain-words hint, never hidden), the trust line verbatim; picking a server
re-points the lobby socket + boots via the existing `?server=<url>` path
with zero main.js changes. test-ui/global-browse.spec.js: a real master +
two live servers, cross-origin browse proven. **A51 = a QuakeWorld-style
find-a-game pipeline end to end; deployment gated on the user's VM/DNS.**

## marker-0042 — wiki-extract backstop
tools/wiki2data.js TARGET_TITLES += governments + civilizations pages, so
the overlay-authored data/governments.json and hand-maintained
data/civs.json sit under the same wiki-verification net as units/buildings/
wonders. The reviewer's live check found both initial titles were redirect
stubs → fixed to the real pages ('Government (Civ1)', 'Civilizations
(Civ1)') in the follow-up commit; re-check pending. The live check's
stat-drift eyeball also surfaced the freeUnitsPerCity=3 Civ2-shape drift and
exact settler-food values — both now on the user's authenticity menu.

## Also in this span (untagged golden-neutral commits)
Reviewer advisory specs exported (civ24-features, mobile-plan,
match-report-corpus, palette-pass, ruleset-compat-policy), the ally's AI
strategic-modes framework (specs/ai-modes-framework.md), docs/13
visual-fidelity workstream, roblox world-look toggle + typeExtra manifests,
the leader-attributes Sun-Tzu annotation, and the node-v24 test-runner fix
(true test count 465 — the dir form had counted 3 phantom helper files).

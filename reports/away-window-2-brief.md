# Welcome back — away window 2, in one page (2026-07-21 evening)

## TL;DR

Markers **0077, 0078, 0079** tagged (+ the XII.5b latch landed, gating now
— 0080 candidate). Two of the three breaking-class gaps you ruled in are
CLOSED: **barbarian sea raids** (with the ally's sails-spotted telegraph)
and **A91 pollution + global warming + meltdown** (authentic-ON + your
toggle). The space arc ran the full measure→tune→measure→escalate loop
twice; the commitment LATCH is in and its 25-seed re-witness is running as
you read. Everything merge-consistent; redeploy from the latest marker.

## Redeploy

`git pull && ./ssh-deploy.sh` — merge candidate = the latest marker
(0079 now; 0080 likely by the time you read this — check the last report).
The live box picks up: sea raids, pollution/warming, the air-truth rules,
545-turn games from last window, all client fixes.

## The space-arc saga (the window's story)

9-metric sweep: space AI non-functional (0/25 launches, 100% abandon —
every-turn peace check killed 150-turn projects) → TUNE (tolerate
skirmishes; abandon only warring/high) → re-witness: commits now SURVIVE
to 67-91% path completion but threat SPIKES still killed all 3 (seed 5:
committed at threat NONE, dead 40 turns later) → LATCH (sustained-threat
hysteresis: abandon only after 6 consecutive high-threat turns; spike
survives, siege abandons — both directions pinned) → third re-witness IN
FLIGHT. If it fails, options come to you with data; the trajectory says
it closes.

## Also this window

- seaRaidChance=4 was DEGENERATE (barbs held 3/12 cities, world collapsed
  t107) — caught by pre-ship sweep, shipped at 8.
- Settler-density question CLOSED: 14 median cities @545t = authentic;
  the "sparse 7.5" was a 400t measurement artifact. No change.
- A91's perf flag self-corrected: the 2-3x slowdown was measurement
  contention; clean runs match baseline. pollution-perf de-prioritized.
- Fact-pack mail-routing gap found+fixed (packs now go to
  coordinator+bugfixer).
- Unit-bloat evidence accumulated (3/25 seeds trip the 1000-unit harness
  cap late-game) — attached to the xiv-ai-behavior batch.

## Wake list (sessions down, in wake order)

1. **sim-runner** — down ~4h. Owed: Gate-B re-runs (0078/0079/latch), the
   roblox combined re-bake commit (gaming-PC clone, ~5h parked), re-witness
   confirmations.
2. **helper** — down ~5h mid-§22 (its ⅔ hover delivery is landed; the
   remaining chunk is cleanly scoped). Queue 23.
3. **hardening** — down ~6h; new-surface self-audit untaken.

## Your standing items (unchanged)

Studio session (Studded + relations panel + tile-props + EditableImage
spike + save runG.txt) · terrain desaturation check · ally relay
(thanks-note + the routed endings confirmation) · redeploy above.

## Pipeline state

Engine queue after the latch: A91c nuke-fallout → disasters → naval-truth
→ NAVAL LOOP (the big one) → difficulty → smalls. All specced/packed.
Helper queue 23 (parked). Design ledger: still empty — nothing awaits
design anywhere; the three in-flight measurement verdicts are the only
open questions, and none needs you unless the re-witness fails.

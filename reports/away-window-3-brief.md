# Away-window 3 brief (2026-07-24, ~7h) — three markers, two arcs closed, one fork for you

Catch-up order: this file → the fork brief below → `reports/marker-0092/0093.md`.
Redeploy candidate: **marker-0094** (`a823613`) — tagged before your
return; the gov arc HALVED unit bloat (77.5->36/civ).

## The window in one paragraph

Three markers tagged (0092/0093, 0094 pending sweep) — 25+ consecutive
consistent. The AI now INVADES overseas (formally accepted: 25/25
seeds, 0 invariant violations, median 10 troops overseas / 35
captures). Both bugs you hit are fixed: the 14-civ regent hang (a
hardcoded guard of 10 — ≥12-civ games stranded the active player,
BOTH platforms) and the rejoin ERROR (both halves: three-way server
answer incl. reload-after-restart + a graceful card with your layout
fix). Your perf complaints produced: ff chunking (no more Firefox
hangs), the age-snapshot baker with browser instant-load (your
14-civ/medium config is a baked preset), and a profile-first perf fix
(~20%) that identified the REAL costs (deepClone 23% + blockade scan
22% — the guessed suspects all cleared). The gov arc (#36) completed:
the AI researches toward republic/democracy (0→2/2 survivors), goes
democracy-if-safe, and MODERNIZES obsolete garrisons (it had never
issued upgradeUnit). Natural games now run the full 545-turn calendar.
XV §7's engine half landed (capital settler-refuse). Two more
never-committed test files were caught by the new untracked-file
augment.

## ⚖ THE FORK (your one ruling): space launches

State of evidence, all measured:
- witness-7 (war-hold + endemic-war fixes in): **0 launches** at King;
  everything mechanical is proven (commits, holds, drains, builds).
- **Bulb-tune is RULED OUT** (#2309): 0 launches even at −30% research
  costs, no floor damage — cost-tuning does not close it.
- FINAL DATUM (#2332): the gov arc lengthened games to the full 545
  calendar AND halved unit bloat — and launches are STILL 0. Research
  depth is definitively the sole blocker; the fork is fully
  characterized.
- OPTIONS: **(a) ACCEPT** — space is an authentically-contested ending
  humans win and the AI rarely completes emergently; every mechanical
  system ships proven. **(b) further war-taming** — the residual
  blocker is that King ecosystems stay at war (gateTechTurn ~465 vs
  the t545 horizon); more peace-seeking (e.g., D4-era AI treaties
  ending wars) might open the window — noting D4–D6 is ALREADY on the
  v1 spine, so (a)-now with a free re-measure after D4 lands is the
  natural staging.
- **My recommendation: (a) accept for v1, re-measure after D4** — no
  new work, no risk, and the D4 re-measure may close it for free.

## Per-lane state at writing

- **bugfixer** — monumental session: invade-B, both bug fixes, perf
  profile + fix 1, the whole gov arc, settler-refuse. Queue 8 (XV §11
  disorder w/ your lux playbook next, then smalls A6a/A8/rehome/
  default-defender, D3-surfacing, D4–D6). The deepClone COW window
  awaits a FRESH session (handoff complete: preopen + 5 conditions +
  mechanism-A ruling).
- **sim-runner** — the away-window measurement engine: naval
  acceptance, fork pre-measure, N1a/arc sweeps, baseline bank live
  (halves every future judge).
- **reviewer** — every gate green same-tick all window; its env-fail
  ledger corrected (the version.js find); clean ledger.
- **helper** — the perf triple + blurb; queue empty; A49 playwright +
  endgame-moments still scoped for a FRESH session.
- **hardening** — rejoin server-half + the version.js catch; drained.
- **roblox-helper** — SESSION DEAD since your Studio session ended;
  its 26-item runI batch + design rulings wait in its queue/inbox.

## NEEDS YOU (in order of leverage)

1. **Rule the fork** (above — one word suffices: accept / war-taming).
2. **Redeploy** from the latest marker (0093 now, 0094 if tagged) —
   carries every fix you hit live this week + the server self-lists
   in Find game.
3. **Restart the roblox-helper session** — 26-item batch + rulings
   waiting; nudge text: _git pull origin dev_night, read mail #2304 +
   your queue #1, self-order the batch blockers-first._
4. **Start a fresh bugfixer session** for the deepClone COW window
   (the 23% perf lever; say: _take the deepClone window per
   debugging/deepclone-map-sharing-preopen.md + mails #2319/#2320_).
5. **Fresh helper session** for A49 playwright + endgame-moments (the
   last axis-6 items).
6. Carried: publish gates (Roblox sound/saving), /mnt/c pump to
   f701d4a+ (lets the reviewer switch to flag-wait), terrain check.

## Post-brief addendum (the window kept landing)

marker-0094 tagged as predicted. ALSO landed after the brief: #7
rehome (gate-green) + #22 the AI disorder playbook (YOUR lux recipe,
built exactly to it: multi-city -> minimum sustainable lux raise
first, K=10 treasury window, entertainer residual) - its gates run
now; marker-0095 = both on greens. The engine queue is down to 6
(default-defender, A6a, A8, pollution-perf remainder, view-contract,
D3-surfacing, D4-D6 tail).

FINAL: marker-0095 also tagged (disorder playbook PASS - 26% disorder
reduction - + rehome). Six markers this window, 0090-0095. Redeploy
from marker-0095.

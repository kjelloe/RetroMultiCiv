# markers 0049–0050 — the space race + the command budget

## marker-0049 — A76 space race (N17, both phases one window)

The fourth target ending lands in the engine. `engine/spaceship.js` + its
byte-shaped twin: the six SS parts as Apollo-gated city production (real
wiki costs/masses/maxima), the integer shipStats model (28/39 structural
sufficiency — reviewer-corrected; flight constants sim-swept FINAL at
1600/15 before any pin committed), launchShip → irreversible flight →
arrival victory with the wiki's exact scoring; capital capture destroys
the ship (rebuildable). AI v1: dormant until a civ reaches space, then
builder civs construct parts in their capitals and EVERYONE rushes a
visible launcher's capital — the race is public by design. Scenario 029
pinned cross-language, constant-independent (year-math keeps the bonus
out of hashed state). Goldens re-recorded once; the natural-run winner
UNCHANGED (space code dormant in the soak — honest). Suite 497/497.
Fill-order ruling: [propulsion, fuel, habitation, lifeSupport, solar] —
flight capability before colonists, matching the red-box visual story.
The H8 graphical screen (shipped mock-first the same night) wires to
these exports at its next touch.

## marker-0050 — A50 item 0: per-connection command budget (fold mode)

The top hosted-surface gap (docs/16 §2.4, measured 1ms→4.5s co-player
starvation under flood) closes: `server/limits.js createCommandBudget` —
per-connection, clock-injectable, O(1) token bucket (burst 40, refill
20/s; CLI knobs), spent BEFORE route() with cheap rateLimited rejects.
Landed as a FOLD into the game stream per the user's ruling, with
docs/17 lane-fencing comments; the hardening lane went LIVE the same
hour and now owns the acceptance A/B + the tuned defaults (the dev-side
corroboration: total starvation → flowing acks; canary p50 0.8s under 6
sustained flooders points at the lower-refill branch). Red-first unit +
ws integration tests; server-only, golden-neutral. Suite 500/500.

## Also in the span (golden-neutral)

C1 world minimap, C2 breakdown tooltips, C3 build queue (A54-powered
off-turn advance), C4 sentry + settler automation (the user's four picks
complete), the browser-feature catalog (Roblox parity contract), the
mobile stranded-in-lobby forensics CLOSED (story A: Android screen-lock
killed the socket pre-start; L8 defensive fixes queued), R24 host-options
parity (all options PRESENT in the Roblox lobby), the government re-eval
spec + pre-design PASS (the payback lever; window open).

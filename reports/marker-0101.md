# marker-0101 — XVII complete, the corrected candidate (MERGE-CONSISTENT)

Tagged at `c51dceb` (2026-07-25, away window). **MERGE-CONSISTENT —
supersedes 0099 and 0100; current merge + redeploy candidate.**
Declared on the reviewer's marker-level clean-clone `--full` GREEN
(#2483: 886 tests, 0 fail; luau 400-turn 0xd4151d33 IDENTICAL across
0097→0101 — four consecutive engine-golden-neutral markers).

## Relation to 0100

0100 tagged the XVII merge with consistency PENDING (the post-0098
flow); the gate found ONE deterministic one-line red — the XVII
swamp-pond prop color missing from the regenerated
specs/render-spec.json (the committed-artifact drift guard working) —
fixed as `c51dceb` (the helper independently produced the identical
fix minutes later; lesson: a new PROP_COLOR regenerates BOTH
asset-recipes AND render-spec). The A29 browser fail paired as a
load-sensitive flake on both 0100 and its parent.

## Content (see reports/marker-0100.md for the full delta)

Refinement-XVII complete (18 client items + join-toggle both halves)
+ runK roblox batch + the agent-mail ack-parser fix + the render-spec
regen. Engine untouched; the spine runway (A8 threading →
coastal-build → RIVER → D3-surfacing → D4–D6) awaits the fresh
bugfixer session.
